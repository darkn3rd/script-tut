# Module Path
# * ../shared_modules - classifier-agnostic modules, shared by all classifier trees
# * ../.forge-vendor - modules downloaded from the Puppet Forge
modulepath = ../shared_modules:../.forge-vendor
