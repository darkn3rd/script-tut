---
lessons:
  ubuntu22:
    id: ubuntu22
    common:
    - type: script
      name: ubuntu22_sdkman
      cmd: |
        if [ ! -d "$HOME/.sdkman" ]; then
          curl -s "https://get.sdkman.io" | bash
          sed -i 's/sdkman_auto_answer=false/sdkman_auto_answer=true/' "$HOME/.sdkman/etc/config"
        fi
        source "$HOME/.sdkman/bin/sdkman-init.sh"
    - type: append
      name: ubuntu22_sdkman
      dest:
      - "$HOME/.bashrc"
      - "$HOME/.zshrc"
      lines:
      - '[[ -s "$HOME/.sdkman/bin/sdkman-init.sh" ]] && source "$HOME/.sdkman/bin/sdkman-init.sh"'
    gen_scripts:
    - type: apt
      name:
      - autoconf
      - bison
      - build-essential
      - libbz2-dev
      - libdb-dev
      - libffi-dev
      - libgdbm6
      - libgdbm-dev
      - liblzma-dev
      - libncurses5-dev
      - libncursesw5-dev
      - libreadline6-dev
      - libreadline-dev
      - libsqlite3-dev
      - libssl-dev
      - libxml2-dev
      - libxmlsec1-dev
      - libyaml-dev
      - llvm
      - tk-dev
      - xz-utils
      - zlib1g-dev
      - gawk
      - php8.1-cli
      - tcl8.6
    - type: script
      name: ubuntu22_rbenv
      cmd: |
        sudo apt-get remove -y rbenv ruby-build 2>/dev/null || true
        curl -fsSL https://github.com/rbenv/rbenv-installer/raw/HEAD/bin/rbenv-installer | bash
        if [[ -d "$HOME/.rbenv/plugins/ruby-build" ]]; then
          pushd "$HOME/.rbenv/plugins/ruby-build" && git pull && popd
        else
          git clone https://github.com/rbenv/ruby-build.git "$HOME/.rbenv/plugins/ruby-build"
        fi
        export PATH="$HOME/.rbenv/bin:$PATH"
        eval "$(rbenv init - bash)"
    - type: append
      name: ubuntu22_rbenv_bash
      dest: "$HOME/.bashrc"
      lines:
      - export PATH="$HOME/.rbenv/bin:$PATH"
      - eval "$(rbenv init - bash)"
    - type: append
      name: ubuntu22_rbenv_zsh
      dest: "$HOME/.zshrc"
      lines:
      - export PATH="$HOME/.rbenv/bin:$PATH"
      - eval "$(rbenv init - zsh)"
    - type: script
      name: ubuntu22_pyenv
      cmd: |
        if [ ! -d "$HOME/.pyenv" ]; then
          curl https://pyenv.run | bash
        fi
        export PYENV_ROOT="$HOME/.pyenv"
        export PATH="$PYENV_ROOT/bin:$PATH"
        eval "$(pyenv init - bash)"
        pyenv update
    - type: append
      name: ubuntu22_pyenv_bash
      dest: "$HOME/.bashrc"
      lines:
      - export PYENV_ROOT="$HOME/.pyenv"
      - '[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"'
      - eval "$(pyenv init - bash)"
    - type: append
      name: ubuntu22_pyenv_zsh
      dest: "$HOME/.zshrc"
      lines:
      - export PYENV_ROOT="$HOME/.pyenv"
      - '[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"'
      - eval "$(pyenv init - zsh)"
    - type: sdkman
      name: groovy
    - type: script
      name: ubuntu22_cpan_local_setup
      cmd: |
        PERL_MM_USE_DEFAULT=1 PERL_MM_OPT="INSTALL_BASE=$HOME/perl5" cpan local::lib
        eval "$(perl -I$HOME/perl5/lib/perl5 -Mlocal::lib=$HOME/perl5)"
        export PATH=$HOME/perl5/bin:$PATH
    - type: append
      name: ubuntu22_cpan_local_setup
      dest:
      - "$HOME/.bashrc"
      - "$HOME/.zshrc"
      lines:
      - eval "$(perl -I$HOME/perl5/lib/perl5 -Mlocal::lib=$HOME/perl5)"
      - export PATH=$HOME/perl5/bin:$PATH
    - type: cpan
      name: App::cpanminus
    - type: cpanm
      name: Switch
    - type: pyenv
      name: 3.14.7
      cmd: pyenv global 3.14.7
    - type: pyenv
      name: 2.7.18
      cmd: pyenv global 3.14.7 2.7.18
    - type: rbenv
      name: 4.0.6
      cmd: rbenv global 4.0.6
    shell_scripts:
    - type: apt
      name:
      - tcsh
      - ksh93u+m
      - zsh
    - type: file
      name: ubuntu_default_zshrc
      dest: "$HOME/.zshrc"
      content: |
        # Set up the prompt

        autoload -Uz promptinit
        promptinit
        prompt adam1

        setopt histignorealldups sharehistory

        # Use emacs keybindings even if our EDITOR is set to vi
        bindkey -e

        # Keep 1000 lines of history within the shell and save it to ~/.zsh_history:
        HISTSIZE=1000
        SAVEHIST=1000
        HISTFILE=~/.zsh_history

        # Use modern completion system
        autoload -Uz compinit
        compinit

        zstyle ':completion:*' auto-description 'specify: %d'
        zstyle ':completion:*' completer _expand _complete _correct _approximate
        zstyle ':completion:*' format 'Completing %d'
        zstyle ':completion:*' group-name ''
        zstyle ':completion:*' menu select=2
        eval "$(dircolors -b)"
        zstyle ':completion:*:default' list-colors ${(s.:.)LS_COLORS}
        zstyle ':completion:*' list-colors ''
        zstyle ':completion:*' list-prompt %SAt %p: Hit TAB for more, or the character to insert%s
        zstyle ':completion:*' matcher-list '' 'm:{a-z}={A-Z}' 'm:{a-zA-Z}={A-Za-z}' 'r:|[._-]=* r:|=* l:|=*'
        zstyle ':completion:*' menu select=long
        zstyle ':completion:*' select-prompt %SScrolling active: current selection at %p%s
        zstyle ':completion:*' use-compctl false
        zstyle ':completion:*' verbose true

        zstyle ':completion:*:*:kill:*:processes' list-colors '=(#b) #([0-9]#)*=0=01;31'
        zstyle ':completion:*:kill:*' command 'ps -u $USER -o pid,%cpu,tty,cputime,cmd'
    compiled_lang:
    - type: apt
      name:
      - openjdk-17-jdk
      - curl
      - build-essential
    - type: apt
      name: dotnet-sdk-10.0
      apt_repository: ppa:dotnet/backports
    - type: apt
      name: golang-go
      apt_repository: ppa:longsleep/golang-backports
    - type: append
      name: ubuntu22_go
      dest:
      - "$HOME/.bashrc"
      - "$HOME/.zshrc"
      lines:
      - export GOPATH=$(go env GOPATH)
      - export PATH=$GOPATH/bin:$PATH
    - type: script
      name: ubuntu22_rust
      cmd: |
        if [ ! -d "$HOME/.cargo" ]; then
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \
            | sh -s -- -y --profile minimal --no-modify-path
        fi
        mkdir -p "$HOME/.config/powershell"
        source "$HOME/.cargo/env"
    - type: file
      name: ubuntu22_rust_ps1
      dest: "$HOME/.cargo/env.ps1"
      content: |
        # rustup shell setup
        # affix colons on either side of $env:PATH to simplify matching
        if (":${env:PATH}:" -notlike "*:$HOME/.cargo/bin:*") {
            # Prepending path in case a system-installed rustc needs to be overridden
            $env:PATH = "$HOME/.cargo/bin:$env:PATH"
        }
    - type: append
      name: ubuntu22_rust_shell
      dest:
      - "$HOME/.bashrc"
      - "$HOME/.zshrc"
      lines:
      - source "$HOME/.cargo/env"
    - type: append
      name: ubuntu22_rust_ps1
      dest: "$HOME/.config/powershell/Microsoft.PowerShell_profile.ps1"
      lines:
      - . "$HOME/.cargo/env.ps1"
    win_scripts:
    - type: script
      name: ubuntu22_powershell
      cmd: |
        wget -q https://packages.microsoft.com/config/ubuntu/$(lsb_release -rs)/packages-microsoft-prod.deb \
          -O packages-microsoft-prod.deb
        sudo DEBIAN_FRONTEND=noninteractive dpkg -i packages-microsoft-prod.deb
        rm packages-microsoft-prod.deb
        sudo apt-get update
        sudo DEBIAN_FRONTEND=noninteractive apt-get install -y powershell
        pwsh -NoProfile -Command "Set-PSRepository -Name PSGallery -InstallationPolicy Trusted; Install-Module -Name psake -Scope CurrentUser -Force -SkipPublisherCheck -AllowClobber"
scriptbox:
  ubuntu22:
    id: ubuntu22
    packages:
    - type: apt
      name:
      - clang
      - libclang-dev
    - type: script
      name: ubuntu22_gem_update
      cmd: "URL=\"https://rubygems.org/api/v1/versions/rubygems-update/latest.json\"\nif
        [[ \"$(curl -s $URL | grep -oP '\"version\"[: ]*\"\\K[^\"]+')\" != \"$(gem
        --version)\" ]]; then \n  gem update --system\nfi\n"
    - type: gem
      name: ratatui_ruby
      args: "--platform ruby"
