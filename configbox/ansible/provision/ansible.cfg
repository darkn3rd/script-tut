[defaults]
# .galaxy-vendor/roles/ - gitignored, fetched on demand (see requirements.yml)
roles_path = .galaxy-vendor/roles

# collections_paths deliberately unset - see Vagrantfile's own galaxy_command
#  comment for why collections don't vendor here the way roles do
