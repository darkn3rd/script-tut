# Bubblegum and Scripts

This section use shell scripts in `powershell` or `bash` to configure a system.  This will be a way to test generated scripts and confgiuration source of truth from **Scriptbox** (see [scriptbox readme](../../scriptbox/README.md)).

The main driver of this tooling will be **[Vagrant](https://developer.hashicorp.com/vagrant)**.  



