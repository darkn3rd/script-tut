# Hand-written - never regenerated. Same shape as ../../static/roots/
#  salt/top.sls and ../../roster/roots/salt/top.sls - the shared
#  lessons tree doesn't care which of the three ways it's being run.
base:
  'ubuntu22':
    - lessons
