#!/usr/bin/node

// integer arithmatic
var width = 5; var length = 6;
var area = width * length;
console.log( "The area of a square(width=" + width + 
              ", length=" + length + ") is " + area);

// boolean logic 
result = true && false || true;
console.log("The statement (true AND false OR true) is " + result);
