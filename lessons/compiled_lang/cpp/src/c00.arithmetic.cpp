#include <iostream>

int main() {
    int width = 5;
    int length = 6;
    int area = width * length;

    std::cout << "The area of a square (width=" << width << ", length=" << length
               << ") is " << area << "." << std::endl;

    return 0;
}
