using System;

class C20Arithmetic
{
    static void Main()
    {
        int radius = 3;
        double area = Math.PI * Math.Pow(radius, 2);

        Console.WriteLine($"The area of a circle (radius={radius}) is: {area}.");
    }
}
