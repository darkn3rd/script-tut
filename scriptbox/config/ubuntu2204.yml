ubuntu22:
  apt_mirror: https://archive.ubuntu.com/ubuntu/
  global:
    packages:
      - cmd: sudo apt update
      - apt: zsh
        file: ubuntu_default_zshrc
      - script: unbuntu22_wsl2_clean
      - apt: ['curl','git','make','wget']
        meets: make
    lessons:
      packages:
        - script: ubuntu22_sdkman
          append: ubuntu22_sdkman
          tags: [sdkman]
      gen_scripts:
        packages:
          # Shared requirements for cpan, rbenv, pyenv
          - apt:
              - autoconf
              - bison
              - build-essential
              - libbz2-dev
              - libdb-dev
              - libffi-dev
              - libgdbm6
              - libgdbm-dev
              - liblzma-dev
              - libncurses5-dev
              - libncursesw5-dev
              - libreadline6-dev
              - libreadline-dev
              - libsqlite3-dev
              - libssl-dev
              - libxml2-dev
              - libxmlsec1-dev
              - libyaml-dev
              - llvm
              - tk-dev
              - xz-utils
              - zlib1g-dev
            tags: [pyenv,rbenv]
            meets: build_essential
          - script: ubuntu22_rbenv
            append: 
              - ubuntu22_rbenv_bash
              - ubuntu22_rbenv_zsh
            tags: [rbenv]
            meets: rbenv
            needs: build_essential
          - script: ubuntu22_pyenv
            append: 
              - ubuntu22_pyenv_bash
              - ubuntu22_pyenv_zsh
            tags: [pyenv]
        awk:
          packages:
            - apt: gawk
        groovy:
          packages:
            - sdkman: groovy
              needs: java
              tags: [sdkman]
        perl:
          packages:
            - system: perl
              meets: perl
              script: ubuntu22_cpan_local_setup
              append: ubuntu22_cpan_local_setup
              needs: make
            - cpan: App::cpanminus
            - cpanm: Switch
        php:
          packages:
            - apt: php8.1-cli
        python2:
          packages:
            - pyenv: 2.7.18
              cmd: pyenv global 3.14.7 2.7.18
              needs: pyenv3
              tags: [pyenv,python2]
        python3:
          packages:
            - system: python # 3.10.x
            - pyenv: 3.14.7
              cmd: pyenv global 3.14.7
              meets: pyenv3
              tags: [pyenv]
        ruby:
          packages:
            - system: ruby # 3.0.x
            - rbenv: 4.0.6
              cmd: rbenv global 4.0.6
              tags: [rbenv]
              meets: ruby
              needs: rbenv
        tcl:
          packages:
            - apt: tcl8.6
      shell_scripts:
        bash:
            packages: 
              - system: bash
        csh:
            packages: 
              - apt: tcsh
        ksh:
            packages: 
              - apt: ksh93u+m
        posix:
            packages: 
              - system: dash
              - needs: perl
        zsh:
            packages:
            - apt: zsh
              file: ubuntu_default_zshrc
      compiled_lang:
        packages:
          - apt: curl
          - apt: build-essential
            meets: make
        cs:
          packages:
            - apt_repository: ppa:dotnet/backports
              apt: dotnet-sdk-10.0
        java:
          packages:
            - apt: openjdk-17-jdk
              meets: java
        go:
          packages:
            - apt_repository: ppa:longsleep/golang-backports
              apt: golang-go
              append: ubuntu22_go
        rust:
          packages:
            - script: ubuntu22_rust
              append:
                - ubuntu22_rust_shell
                - ubuntu22_rust_ps1
              file: ubuntu22_rust_ps1
      win_scripts:
        powershell:
          packages:
            - script: ubuntu22_powershell
              meets: powershell
    cibox:
      packages:
        - add_apt_repo: ubuntu22_docker
        - apt: 
            - docker-ce
            - docker-ce-cli
            - containerd.io
            - docker-buildx-plugin
            - docker-compose-plugin
        - cmd: curl https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash
    pkgbox:
      packages:
        - add_apt_repo: ubuntu22_nfpm
        - apt: nfpm
    scriptbox:
      packages:
        - script: ubuntu22_gem_update
          needs: ruby
          tags: [rbenv]
        - apt: [clang, libclang-dev]
          # Skip incompatible precompiled binary with glibc 2.39
          # and compile from source using glibc 2.35
        - gem: ratatui_ruby
          args: --platform ruby
    testbox:
      packages:
        - cmd: pwsh --version
          needs: powershell
        - cmd: ruby --version
          needs: ruby

scripts:
  ubuntu22_gem_update:
    type: bash
    cmd: |
      URL="https://rubygems.org/api/v1/versions/rubygems-update/latest.json"
      if [[ "$(curl -s $URL | grep -oP '"version"[: ]*"\K[^"]+')" != "$(gem --version)" ]]; then 
        gem update --system
      fi

  ubuntu22_rust:
    type: bash
    cmd: |
      # Skip if already installed - re-provisioning shouldn't re-run this
      # or duplicate the profile lines below on every pass.
      if [ ! -d "$HOME/.cargo" ]; then
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \
          | sh -s -- -y --profile minimal --no-modify-path
      fi
      mkdir -p "$HOME/.config/powershell"
      source "$HOME/.cargo/env"
  ubuntu22_powershell:
    type: bash
    cmd: |
      wget -q https://packages.microsoft.com/config/ubuntu/$(lsb_release -rs)/packages-microsoft-prod.deb \
        -O packages-microsoft-prod.deb
      sudo DEBIAN_FRONTEND=noninteractive dpkg -i packages-microsoft-prod.deb
      rm packages-microsoft-prod.deb

      # Add Microsoft packages list
      sudo apt-get update

      # Install PowerShell 7.x (pwsh)
      sudo DEBIAN_FRONTEND=noninteractive apt-get install -y powershell

      # psake - win_scripts/powershell lessons run through Invoke-Psake
      #  (see scriptbox/scripts/run_all_tests.ps1), which needs the
      #  psake module installed. A plain Install-Module prompts
      #  "Untrusted repository... Are you sure you want to install
      #  software from this untrusted source?" the first time PSGallery
      #  is used - trust it first, then -Force/-SkipPublisherCheck skip
      #  that confirmation non-interactively.
      pwsh -NoProfile -Command "Set-PSRepository -Name PSGallery -InstallationPolicy Trusted; Install-Module -Name psake -Scope CurrentUser -Force -SkipPublisherCheck -AllowClobber"
  ubuntu22_pyenv:
    type: bash
    cmd: |
      # pyenv.run refuses to run again once $HOME/.pyenv exists
      #  skip entirely instead of catching an error
      if [ ! -d "$HOME/.pyenv" ]; then
        curl https://pyenv.run | bash
      fi

      export PYENV_ROOT="$HOME/.pyenv"
      export PATH="$PYENV_ROOT/bin:$PATH"
      eval "$(pyenv init - bash)"

      # Update List
      pyenv update
  ubuntu22_rbenv:
    type: bash
    cmd: |
      # Ubuntu package issue: 
      #   * rbenv/ruby-build apt packages install to /usr/bin, a completely 
      #     different, incompatible layout than the ~/.rbenv/
      #   * rbenv version list frozen frozen at an April 2020
      #     snapshot (`ruby-build 20200401-1build1`, and only supports 
      #     up to 2.7.1
      sudo apt-get remove -y rbenv ruby-build 2>/dev/null || true

      # rbenv-installer is idempotent and will install fresh or update
      #   rebenv in place under $HOME/.rbenv
      curl -fsSL https://github.com/rbenv/rbenv-installer/raw/HEAD/bin/rbenv-installer | bash

      # ruby-build plugin is installed or updated using git
      if [[ -d "$HOME/.rbenv/plugins/ruby-build" ]]; then
        pushd "$HOME/.rbenv/plugins/ruby-build" && git pull && popd
      else
        git clone https://github.com/rbenv/ruby-build.git "$HOME/.rbenv/plugins/ruby-build"
      fi

      export PATH="$HOME/.rbenv/bin:$PATH"
      eval "$(rbenv init - bash)"
  ubuntu22_cpan_local_setup:
    type: bash
    cmd: |
      #  Force ExtUtils::MakeMaker's own prompt() to silently accept
      #    every default answer instead of blocking on stdin.
      PERL_MM_USE_DEFAULT=1 PERL_MM_OPT="INSTALL_BASE=$HOME/perl5" cpan local::lib

      eval "$(perl -I$HOME/perl5/lib/perl5 -Mlocal::lib=$HOME/perl5)"
      export PATH=$HOME/perl5/bin:$PATH
  unbuntu22_wsl2_clean:
    type: sh
    cmd: |
      if grep -q "microsoft" /proc/version; then
        CLEAN_PATH="$(echo "$PATH" | tr ':' '\n' | grep -v '/mnt/c/' | tr '\n' ':' | sed 's/:$//')"
        CLEAN_LINE="export PATH=$CLEAN_PATH:/mnt/c/Windows/system32"
        grep -qxF "$CLEAN_LINE" ~/.bashrc 2>/dev/null || printf "\n%s\n" "$CLEAN_LINE" >> ~/.bashrc
        [[ -f $HOME/.zshrc ]] && { grep -qxF "$CLEAN_LINE" ~/.zshrc 2>/dev/null || printf "\n%s\n" "$CLEAN_LINE" >> ~/.zshrc; }
      fi
  ubuntu22_sdkman:
    type: bash
    cmd: |
      # Skip if already installed - see ubuntu22_pyenv's own comment on why.
      if [ ! -d "$HOME/.sdkman" ]; then
        curl -s "https://get.sdkman.io" | bash
        sed -i 's/sdkman_auto_answer=false/sdkman_auto_answer=true/' "$HOME/.sdkman/etc/config"
      fi

      source "$HOME/.sdkman/bin/sdkman-init.sh"

files:
  ubuntu_default_zshrc:
    dest: $HOME/.zshrc
    content: |
      # Set up the prompt

      autoload -Uz promptinit
      promptinit
      prompt adam1

      setopt histignorealldups sharehistory

      # Use emacs keybindings even if our EDITOR is set to vi
      bindkey -e

      # Keep 1000 lines of history within the shell and save it to ~/.zsh_history:
      HISTSIZE=1000
      SAVEHIST=1000
      HISTFILE=~/.zsh_history

      # Use modern completion system
      autoload -Uz compinit
      compinit

      zstyle ':completion:*' auto-description 'specify: %d'
      zstyle ':completion:*' completer _expand _complete _correct _approximate
      zstyle ':completion:*' format 'Completing %d'
      zstyle ':completion:*' group-name ''
      zstyle ':completion:*' menu select=2
      eval "$(dircolors -b)"
      zstyle ':completion:*:default' list-colors ${(s.:.)LS_COLORS}
      zstyle ':completion:*' list-colors ''
      zstyle ':completion:*' list-prompt %SAt %p: Hit TAB for more, or the character to insert%s
      zstyle ':completion:*' matcher-list '' 'm:{a-z}={A-Z}' 'm:{a-zA-Z}={A-Za-z}' 'r:|[._-]=* r:|=* l:|=*'
      zstyle ':completion:*' menu select=long
      zstyle ':completion:*' select-prompt %SScrolling active: current selection at %p%s
      zstyle ':completion:*' use-compctl false
      zstyle ':completion:*' verbose true

      zstyle ':completion:*:*:kill:*:processes' list-colors '=(#b) #([0-9]#)*=0=01;31'
      zstyle ':completion:*:kill:*' command 'ps -u $USER -o pid,%cpu,tty,cputime,cmd'
  # Construct missing env.ps1 based on pattern in .bashrc/.zshrc
  # Issue:
  #   * Rustup post-install message tells PowerShell users to source
  #     $HOME/.cargo/env.ps1 that doesn't exist
  ubuntu22_rust_ps1:
     dest: $HOME/.cargo/env.ps1
     content: |
        # rustup shell setup
        # affix colons on either side of $env:PATH to simplify matching
        if (":${env:PATH}:" -notlike "*:$HOME/.cargo/bin:*") {
            # Prepending path in case a system-installed rustc needs to be overridden
            $env:PATH = "$HOME/.cargo/bin:$env:PATH"
        }

appends:
  ubuntu22_go:
    dest: ["$HOME/.bashrc", "$HOME/.zshrc"]
    lines:
      - export GOPATH=$(go env GOPATH)
      - export PATH=$GOPATH/bin:$PATH
  ubuntu22_sdkman:
    dest: ["$HOME/.bashrc", "$HOME/.zshrc"]
    lines:
      - '[[ -s "$HOME/.sdkman/bin/sdkman-init.sh" ]] && source "$HOME/.sdkman/bin/sdkman-init.sh"'
  ubuntu22_rbenv_bash:
    dest: "$HOME/.bashrc"
    lines:
      - export PATH="$HOME/.rbenv/bin:$PATH"
      - eval "$(rbenv init - bash)"
  ubuntu22_rbenv_zsh:
    dest: "$HOME/.zshrc"
    lines:
      - export PATH="$HOME/.rbenv/bin:$PATH"
      - eval "$(rbenv init - zsh)"
  ubuntu22_pyenv_bash:
    dest: "$HOME/.bashrc"
    lines:
      - export PYENV_ROOT="$HOME/.pyenv"
      - '[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"'
      - eval "$(pyenv init - bash)"
  ubuntu22_pyenv_zsh:
    dest: "$HOME/.zshrc"
    lines:
      - export PYENV_ROOT="$HOME/.pyenv"
      - '[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"'
      - eval "$(pyenv init - zsh)"
  ubuntu22_rust_shell:
    dest: ["$HOME/.bashrc", "$HOME/.zshrc"]
    lines:
      - source "$HOME/.cargo/env"
  ubuntu22_rust_ps1:
    dest: $HOME/.config/powershell/Microsoft.PowerShell_profile.ps1
    lines:
      - . "$HOME/.cargo/env.ps1"
  ubuntu22_coretto_17:
    dest: ["$HOME/.bashrc", "$HOME/.zshrc"]
    lines:
      - export JAVA_HOME=$(readlink -f /usr/bin/java | sed 's|/bin/java||')
  ubuntu22_cpan_local_setup:
    dest: ["$HOME/.bashrc", "$HOME/.zshrc"]
    lines:
      - eval "$(perl -I$HOME/perl5/lib/perl5 -Mlocal::lib=$HOME/perl5)"
      - export PATH=$HOME/perl5/bin:$PATH

add_apt_repos:
  ubuntu22_corretto:
    name: corretto
    key_url: https://apt.corretto.aws/corretto.key
    repo_uri: https://apt.corretto.aws
    distro_string: stable main
  ubuntu22_docker:
    name: docker
    key_url: https://download.docker.com/linux/ubuntu/gpg
    repo_uri: https://download.docker.com/linux/ubuntu
    distro_string: "jammy stable"
  ubuntu22_nfpm:
    name: goreleaser
    trusted: yes
    repo_uri: https://repo.goreleaser.com/apt/
    distro_string: "/"
