#!/usr/bin/env perl -w
# count style loop using while
my $count=10;                   # initialize counter
while ($count > 0) {
   print "Count is $count\n";   # output count
   $count--;                    # decrement count
}
