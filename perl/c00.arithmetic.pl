#!/usr/bin/perl -w
# declare variables
my ($width, $length) = (5,6);

# calculate area
my $area=$length * $width;

# output results
print "The area of a square (width=$width, length=$length) is $area.\n";