using System;

class C00Arithmetic
{
    static void Main()
    {
        int width = 5;
        int length = 6;
        int area = width * length;

        Console.WriteLine($"The area of a square (width={width}, length={length}) is {area}.");
    }
}
